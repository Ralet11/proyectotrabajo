# API Notes

La documentación viva de endpoints se expondrá vía Swagger desde `apps/api`.

Este directorio queda reservado para:

- snapshots de contratos si hubiera integraciones externas
- guías de consumo por frontend
- notas de versionado de la API

